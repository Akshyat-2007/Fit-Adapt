# Application Port
PORT=3000

# Environment Mode (production or development)
NODE_ENV=production

# JWT Authentication Secret Key (generate a random 32+ character string)
JWT_SECRET=your_super_secret_jwt_key_fitadapt_2026

# Database Configuration (Leave blank or omit if using built-in SQLite auto-fallback)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=fitadapt
DB_PORT=3306
